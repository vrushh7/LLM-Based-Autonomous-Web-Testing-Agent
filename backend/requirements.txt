# Core Backend Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# Browser Automation
playwright==1.40.0

# LLM Integration
requests==2.31.0

# Async HTTP (for monitoring and downloads)
aiohttp>=3.8.0

# Utilities
python-dotenv==1.0.0
python-multipart==0.0.6

# CORS Support
fastapi-cors==0.0.6

# Date/Time
python-dateutil==2.8.2

# Screenshot and Image Processing
Pillow==10.1.0

# OCR Support (for smart login fallback and visual verification)
pytesseract>=0.3.10

# Async File Support
aiofiles==23.2.1

# RAG / Vector Database (for learning from past successes)
chromadb>=0.4.0

# Sentence Transformers (for semantic search in RAG)
sentence-transformers>=2.2.0

# WebSocket Support (for live monitoring)
websockets>=12.0

# Excel/CSV Export (for reports)
openpyxl>=3.1.0

# HTML Parsing (for flight and product extraction)
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Optional: For better JSON handling
orjson>=3.9.0

# Optional: For rate limiting and caching
cachetools>=5.3.0